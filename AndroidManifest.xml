<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sengstudio1a">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".controller.LoginController">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--<activity android:name=".controller.LoginController"/>-->
        <activity android:name=".controller.RegisterCharityController"/>
        <activity android:name=".controller.RegisterDonorController"/>
        <activity android:name=".controller.RegisterSelectController"/>
        <activity android:name=".controller.RegisterSuccessController"/>
        <activity android:name=".Placeholder"/>
    </application>

</manifest>